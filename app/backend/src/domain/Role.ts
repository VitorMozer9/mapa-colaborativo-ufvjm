export enum Role {
  // export = permite importar em outros arquivos
  // enum = tipo especial de lista de constantes
  
  ADMIN = 'admin',      
  STUDENT = 'student',  
  TEACHER = 'teacher',  
  VISITOR = 'visitor'   
}